name: ishara
description: "مترجم لغة الإشارة العربية"

version: 1.0.0+1

environment:
  sdk: ^3.13.0


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.9
  provider: ^6.1.5+1
  responsive_framework: ^1.5.1
  camera: ^0.12.0+2
  hand_detection: ^4.1.0
  flutter_tts: ^4.2.5
  permission_handler: ^11.4.0
  path_provider: ^2.1.6
  drift: ^2.14.0
  drift_flutter: ^0.1.0
  sqlite3_flutter_libs: ^0.5.0
  sqflite: ^2.3.0
  http: ^1.2.0
  crypto: ^3.0.0
  tflite_flutter: ^0.12.1
  llama_flutter_android: 0.2.6
  google_mlkit_pose_detection: ^0.16.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  drift_dev: ^2.14.0
  build_runner: ^2.4.0


flutter:

  uses-material-design: true

  assets:
    - assets/models/
